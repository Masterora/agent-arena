# Docker Compose 环境变量示例（复制为 .env 后按需修改）
# 用于 docker-compose.yml 中的变量替换，避免敏感信息写死在 YAML 中。

# PostgreSQL
POSTGRES_USER=agent_arena
POSTGRES_PASSWORD=arena_password_2024
POSTGRES_DB=agent_arena

# Redis
REDIS_PASSWORD=redis_password_2024

# 后端
SECRET_KEY=your-super-secret-key-change-in-production

# 数据卷根路径（Windows 示例: D:\docker-volume\agent-arena；Linux: ./data/volumes）
DOCKER_VOLUME_BASE=./data/volumes

# pgAdmin（可选）
PGADMIN_DEFAULT_EMAIL=admin@arena.com
PGADMIN_DEFAULT_PASSWORD=admin123
